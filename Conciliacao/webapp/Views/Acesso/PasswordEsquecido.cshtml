@{
    ViewBag.PageId = "extr-page";
    ViewBag.Title = "Amplie sua mensagem";
}

@section topright{
    <span id="extr-page-header-space"> <span class="hidden-mobile">Já é cadastrado?</span> <a href="@Url.Action("login", "sistema")" class="btn btn-danger">Entre</a> </span>
}

<div id="content" class="container">
    <div class="row">
        @{ Html.RenderPartial("~/views/acesso/_LoginText.cshtml"); }
        <div class="col-xs-12 col-sm-12 col-md-5 col-lg-4">
            <div class="well no-padding">
                <form action="@Url.Action("forgotpassword", "sistema")" method="POST" id="login-form" class="smart-form client-form">
                    <header>
                        Lembrar senha
                    </header>
                    @Html.AntiForgeryToken()
                    @Html.ValidationBootstrap()
                    <fieldset>
                        <section>
                            <label class="label">Seu E-mail</label>
                            <label class="input">
                                <i class="icon-append fa fa-envelope"></i>
                                <input type="email" name="email">
                                <b class="tooltip tooltip-top-right"><i class="fa fa-user txt-color-teal"></i> Por favor indique aqui o email vinculado a sua conta</b>
                            </label>
                            <div class="note">
                                <a href="@Url.Action("login", "sistema")"><i class="fa fa-smile-o"></i> Opa, eu me lembrei da senha!</a>
                            </div>
                        </section>
                    </fieldset>
                    <footer>
                        <button type="submit" class="btn btn-primary">
                            <i class="fa fa-envelope"></i> 
                            Enviar email
                        </button>
                    </footer>
                </form>
            </div>
          <!--{ Html.RenderPartial("~/views/acesso/_socialmedia.cshtml"); }-->
        </div>
    </div>
</div>
@section pagespecific{
    <script type="text/javascript">
        $(function () {
            // Validation
            $("#login-form").validate({
                // Rules for form validation
                rules: {
                    email: {
                        required: true,
                        email: true
                    },
                },

                // Messages for form validation
                messages: {
                    email: {
                        required: 'Please enter your email address',
                        email: 'Please enter a VALID email address'
                    },
                },

                // Do not change code below
                errorPlacement: function (error, element) {
                    error.insertAfter(element.parent());
                }
            });
        });
    </script>
}